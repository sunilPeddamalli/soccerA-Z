<% layout('/layout/boilerplate') %> 
<body>
    <div class="row mt-3">
        <div class="col-6 offset-3">
            <div class="card">                
                <div class="card-body">     
                  <h5 class="card-title d-flex justify-content-center"><%= match.team1%> <span class='mx-2 px-1 btn-success'><%= match.score1 %> - <%= match.score2 %></span> <%= match.team2 %></h5>
                  <p class="card-text input-group d-flex justify-content-evenly">
                      <small class="form-control border-0" >
                          <% for(scorer of goalScorer1){ %> 
                              <small class='d-block d-flex justify-content-end text-muted'><% if(scorer !=='') {%> ⛹️‍♂️ <% } %>   <%= scorer %></small>
                            <% } %> 
                      </small>
                      <span class="input-group-text bg-white border-0">⚽</span>
                      <small class="form-control border-0" >
                          <% for(scorer of goalScorer2){ %> 
                            <small class='d-block d-flex justify-content-start text-muted'><% if(scorer !=='') {%> ⛹️‍♂️ <% } %>  <%= scorer %></small>
                          <% } %> 
                      </small>
                    </p>
                </div>

                <div class=' list-group  text-muted text-center'>
                  <small class='mt-2'>Player of the Match</small>
                  <small class=' d-block text-muted fw-bold mb-2'>✨<%= match.playerOfTheMatch %> </small>
                </div>

                <img src="<%= match.image %>" class="card-img" alt="...">

                <ul class="list-group list-group-flush">
                  <li class="list-group-item">🏆 <%= match.title %></li>
                  <li class="list-group-item">📅 <%= match.date %></li>
                  <li class="list-group-item">🏁 <%= match.location %></li>
                </ul>
                
                <div class="card-body">
                  <a href="/matches/<%= match._id %>/edit" class="btn btn-info">Edit</a>
                  <form action="/matches/<%=match._id%>?_method=DELETE" method="POST" class='d-inline'>
                    <button class='btn btn-danger'>Delete</button>
                  </form>
                </div>
            </div>
        </div>
    </div>
</body>
